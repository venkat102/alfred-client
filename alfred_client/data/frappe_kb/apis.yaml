# Auto-generated by alfred_client/scripts/build_kb_apis.py
# Edit apis_overrides.yaml to curate individual entries; those win on merge.
# DO NOT hand-edit this file; re-run the script after upstream changes.

api_frappe_bold:
  kind: api
  title: frappe.bold
  summary: Return `text` wrapped in `<strong>` tags.
  keywords:
  - bold
  - frappe
  - frappe.bold
  signature: 'frappe.bold(text: str | int | float) -> str'
  body: Return `text` wrapped in `<strong>` tags.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_copy_doc:
  kind: api
  title: frappe.copy_doc
  summary: No_copy fields also get copied.
  keywords:
  - copy
  - copy_doc
  - doc
  - frappe
  - frappe.copy_doc
  signature: 'frappe.copy_doc(doc: ''Document'', ignore_no_copy: bool = True) -> ''Document'''
  body: No_copy fields also get copied.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_db_after_commit:
  kind: api
  title: frappe.db.after_commit
  summary: Manage callbacks.
  keywords:
  - after
  - after_commit
  - commit
  - frappe
  - frappe.db
  - frappe.db.after_commit
  signature: 'frappe.db.after_commit(func: collections.abc.Callable) -> None'
  body: |-
    Manage callbacks.

    ```
    # Capture callacks
    callbacks = CallbackManager()

    # Put a function call in queue
    callbacks.add(func)

    # Run all pending functions in queue
    callbacks.run()

    # Reset queue
    callbacks.reset()
    ```

    Example usage: frappe.db.after_commit
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_db_before_commit:
  kind: api
  title: frappe.db.before_commit
  summary: Manage callbacks.
  keywords:
  - before
  - before_commit
  - commit
  - frappe
  - frappe.db
  - frappe.db.before_commit
  signature: 'frappe.db.before_commit(func: collections.abc.Callable) -> None'
  body: |-
    Manage callbacks.

    ```
    # Capture callacks
    callbacks = CallbackManager()

    # Put a function call in queue
    callbacks.add(func)

    # Run all pending functions in queue
    callbacks.run()

    # Reset queue
    callbacks.reset()
    ```

    Example usage: frappe.db.after_commit
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_db_count:
  kind: api
  title: frappe.db.count
  summary: Return `COUNT(*)` for given DocType and filters.
  keywords:
  - count
  - frappe
  - frappe.db
  - frappe.db.count
  signature: 'frappe.db.count(dt, filters=None, debug=False, cache=False, distinct:
    bool = True)'
  body: Return `COUNT(*)` for given DocType and filters.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_db_escape:
  kind: api
  title: frappe.db.escape
  summary: Escape quotes and percent in given string.
  keywords:
  - escape
  - frappe
  - frappe.db
  - frappe.db.escape
  signature: frappe.db.escape(s, percent=True)
  body: Escape quotes and percent in given string.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_db_exists:
  kind: api
  title: frappe.db.exists
  summary: |
    Check if a document exists without loading it. Returns the name string if found, None otherwise. Use before get_doc to avoid DoesNotExistError.
  signature: frappe.db.exists(doctype, name_or_filters=None, cache=False) -> str |
    None
  keywords:
  - exists
  - db
  - check
  - has
  - present
  - lookup
  body: |
    Two shapes:

    1. By primary key:
       if frappe.db.exists("Customer", "CUST-0001"):
           ...

    2. By filter dict:
       existing = frappe.db.exists("Contact", {"email_id": "alice@x.com"})
       # -> "contact-name" or None

    Returns the document NAME (truthy string) when found, None otherwise.
    Pattern-compatible with `if frappe.db.exists(...):`.
  examples:
    good: |
      if not frappe.db.exists("Item", item_code):
          frappe.throw(f"Item {item_code} not found")
    bad: |
      # Don't count for existence - use exists()
      if frappe.db.count("Item", {"item_code": x}) > 0:
          ...   # two extra joins compared to exists()
  verified_on: '2026-04-16'

api_frappe_db_get_all:
  kind: api
  title: frappe.db.get_all
  summary: (Auto-scraped from frappe.db; no upstream docstring.)
  keywords:
  - all
  - frappe
  - frappe.db
  - frappe.db.get_all
  - get
  - get_all
  signature: frappe.db.get_all(*args, **kwargs)
  body: (Auto-scraped from frappe.db; no upstream docstring.)
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_db_get_default:
  kind: api
  title: frappe.db.get_default
  summary: Return default value as a list if multiple or single.
  keywords:
  - default
  - frappe
  - frappe.db
  - frappe.db.get_default
  - get
  - get_default
  signature: frappe.db.get_default(key, parent='__default')
  body: Return default value as a list if multiple or single.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_db_get_list:
  kind: api
  title: frappe.db.get_list
  summary: (Auto-scraped from frappe.db; no upstream docstring.)
  keywords:
  - frappe
  - frappe.db
  - frappe.db.get_list
  - get
  - get_list
  - list
  signature: frappe.db.get_list(*args, **kwargs)
  body: (Auto-scraped from frappe.db; no upstream docstring.)
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_db_get_single_value:
  kind: api
  title: frappe.db.get_single_value
  summary: Get property of Single DocType. Cache locally by default
  keywords:
  - frappe
  - frappe.db
  - frappe.db.get_single_value
  - get
  - get_single_value
  - single
  - value
  signature: 'frappe.db.get_single_value(doctype: str, fieldname: str, cache: bool
    = True, *, debug=False, for_update=False, run=True)'
  body: |-
    Get property of Single DocType. Cache locally by default

    :param doctype: DocType of the single object whose value is requested
    :param fieldname: `fieldname` of the property whose value is requested

    Example:

            # Get the default value of the company from the Global Defaults doctype.
            company = frappe.db.get_single_value('Global Defaults', 'default_company')
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_db_get_value:
  kind: api
  title: frappe.db.get_value
  summary: |
    Read one or more fields from a document WITHOUT loading it. Cheap, permission-respecting, returns None when the doc doesn't exist.
  signature: frappe.db.get_value(doctype, name_or_filters, fieldname='name', as_dict=False,
    ...) -> scalar | tuple | dict | None
  keywords:
  - get_value
  - db
  - read
  - query
  - field
  - cheap
  - lookup
  body: |
    Three common shapes:

    1. Single field, by name:
       email = frappe.db.get_value("User", "alice@x.com", "email")
       # -> "alice@x.com" or None

    2. Multiple fields, by name:
       name, email = frappe.db.get_value("User", "alice@x.com", ["full_name", "email"])
       # returns a tuple in list order

    3. With filters + as_dict:
       row = frappe.db.get_value(
           "User",
           {"enabled": 1, "username": "alice"},
           ["name", "email"],
           as_dict=True,
       )
       # -> {"name": "alice@x.com", "email": "alice@x.com"} or None

    Cheaper than frappe.get_doc when you only need a few fields.
    Returns None (not raises) when no document matches.
  examples:
    good: |
      # Guard before acting
      if frappe.db.get_value("Customer", doc.customer, "disabled"):
          frappe.throw("Cannot create orders for disabled customers")
    bad: |
      # Don't load the full doc to read one field
      c = frappe.get_doc("Customer", doc.customer)
      if c.disabled:
          frappe.throw("...")
  pitfalls:
  - Returns None when not found - always handle the None case
  - Field defaults to 'name' - pass an explicit fieldname if you want something else
  - as_dict=True is ONLY useful with a list of fieldnames; passing one fieldname returns
    the value directly
  related:
  - api_frappe_db_set_value
  - api_frappe_db_exists
  verified_on: '2026-04-16'

api_frappe_db_set_value:
  kind: api
  title: frappe.db.set_value
  summary: |
    Update one or more fields on an existing document WITHOUT loading it. Faster than doc.save() for single-field updates but bypasses lifecycle hooks (before_save, validate, etc.). Use with care.
  signature: frappe.db.set_value(doctype, name_or_filters, fieldname, value=None,
    update_modified=True)
  keywords:
  - set_value
  - db
  - update
  - write
  - field
  body: |
    Two shapes:

    1. Single field:
       frappe.db.set_value("ToDo", doc.name, "status", "Closed")

    2. Multiple fields (pass a dict as fieldname):
       frappe.db.set_value("ToDo", doc.name, {"status": "Closed", "date": today})

    Skips doc lifecycle hooks. If you need validate/before_save to run,
    load with frappe.get_doc and call .save() instead.
  examples:
    good: |
      # Bulk status flip, no need for per-doc hooks
      for name in outstanding_names:
          frappe.db.set_value("Invoice", name, "status", "Overdue")
    bad: |
      # Don't use set_value if you need validation / hooks to fire
      frappe.db.set_value("Sales Order", name, "grand_total", 0)
      # (skips every validator on Sales Order - total could be wrong)
  pitfalls:
  - Bypasses lifecycle hooks - validate/before_save do NOT run
  - No permission check by default on write - trust but verify
  related:
  - api_frappe_db_get_value
  verified_on: '2026-04-16'

api_frappe_db_sql:
  kind: api
  title: frappe.db.sql
  summary: |
    Run a raw SQL query. Use sparingly - almost every case is better served by frappe.get_all / frappe.db.get_value. Parameterize user inputs.
  signature: frappe.db.sql(query, values=(), as_dict=False, as_list=False, ...) ->
    list[tuple] | list[dict]
  keywords:
  - sql
  - db
  - query
  - raw
  - select
  body: |
    Two shapes:

    1. Positional params (tuple):
       rows = frappe.db.sql(
           "SELECT name FROM `tabUser` WHERE enabled = %s",
           (1,),
           as_dict=True,
       )

    2. Named params (dict):
       rows = frappe.db.sql(
           "SELECT name FROM `tabUser` WHERE enabled = %(enabled)s",
           {"enabled": 1},
           as_dict=True,
       )

    ALWAYS parameterize. NEVER build SQL with f-strings or concatenation -
    you'll get a SQL injection bug and our security audit will catch it.

    Prefer frappe.get_all for simple SELECTs with filters; reach for sql()
    only when you need JOINs, aggregations, or DB functions Frappe doesn't
    expose directly.
  examples:
    good: |
      rows = frappe.db.sql(
          "SELECT customer, SUM(grand_total) AS total "
          "FROM `tabSales Order` "
          "WHERE docstatus = 1 AND transaction_date >= %s "
          "GROUP BY customer",
          (cutoff_date,),
          as_dict=True,
      )
    bad: |
      # NEVER - SQL injection
      rows = frappe.db.sql(f"SELECT * FROM `tabUser` WHERE name = '{username}'")
  pitfalls:
  - String interpolation = injection bug. Always pass values as the 2nd arg
  - Bypasses Frappe's permission system - the query returns whatever MySQL returns
  - 'Table names need backticks: `tabSales Order`, not `Sales Order`'
  related:
  - api_frappe_db_get_value
  - api_frappe_get_all
  verified_on: '2026-04-16'

api_frappe_delete_doc:
  kind: api
  title: frappe.delete_doc
  summary: Delete a document. Calls `frappe.model.delete_doc.delete_doc`.
  keywords:
  - delete
  - delete_doc
  - doc
  - frappe
  - frappe.delete_doc
  signature: 'frappe.delete_doc(doctype: str | None = None, name: str | dict | None
    = None, force: bool = False, ignore_doctypes: list[str] | None = None, for_reload:
    bool = False, ignore_permissions: bool = False, flags: frappe.types.frappedict._dict
    | None = None, ignore_on_trash: bool = False, ignore_missing: bool = True, delete_permanently:
    bool = False)'
  body: |-
    Delete a document. Calls `frappe.model.delete_doc.delete_doc`.

    :param doctype: DocType of document to be delete.
    :param name: Name of document to be delete.
    :param force: Allow even if document is linked. Warning: This may lead to data integrity errors.
    :param ignore_doctypes: Ignore if child table is one of these.
    :param for_reload: Call `before_reload` trigger before deleting.
    :param ignore_permissions: Ignore user permissions.
    :param delete_permanently: Do not create a Deleted Document for the document.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_enqueue:
  kind: api
  title: frappe.enqueue
  summary: Enqueue method to be executed using a background worker
  keywords:
  - enqueue
  - frappe
  - frappe.enqueue
  signature: 'frappe.enqueue(method: str | collections.abc.Callable, queue: str =
    ''default'', timeout: int | None = None, event: str | None = None, is_async: bool
    = True, job_name: str | None = None, now: bool = False, enqueue_after_commit:
    bool = False, *, on_success: collections.abc.Callable | None = None, on_failure:
    collections.abc.Callable | None = None, at_front: bool = False, job_id: str |
    None = None, deduplicate=False, at_front_when_starved=False, **kwargs) -> rq.job.Job
    | Any'
  body: |-
    Enqueue method to be executed using a background worker

    :param method: method string or method object
    :param queue: should be either long, default or short
    :param timeout: should be set according to the functions
    :param event: this is passed to enable clearing of jobs from queues
    :param is_async: if is_async=False, the method is executed immediately, else via a worker
    :param job_name: [DEPRECATED] can be used to name an enqueue call, which can be used to prevent
    duplicate calls
    :param now: if now=True, the method is executed via frappe.call()
    :param enqueue_after_commit: if True, the job will be enqueued after the current transaction is
    committed
    :param on_success: Success callback
    :param on_failure: Failure callback
    :param at_front: Enqueue the job at the front of the queue or not
    :param kwargs: keyword arguments to be passed to the method
    :param deduplicate: do not re-queue job if it's already queued, requires job_id.
    :param job_id: Assigning unique job id, which can be checked using `is_job_enqueued`
    :param at_front_when_starved: If the queue appears to be starved then new jobs are
    automatically inserted in LIFO fashion.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_get_all:
  kind: api
  title: frappe.get_all
  summary: |
    List documents matching filters. Returns a list of dicts (or plucked values). Respects the caller's read permissions.
  signature: frappe.get_all(doctype, filters=None, fields=None, order_by=None, limit_page_length=20,
    pluck=None, as_list=False, ...) -> list
  keywords:
  - get_all
  - list
  - query
  - filter
  - fields
  - documents
  body: |
    Shapes:

    1. Plain names list:
       names = frappe.get_all("User", filters={"enabled": 1}, pluck="name")
       # -> ["alice", "bob", ...]

    2. Multi-field dict list:
       users = frappe.get_all(
           "User",
           filters={"enabled": 1},
           fields=["name", "email", "full_name"],
           order_by="creation desc",
           limit_page_length=50,
       )
       # -> [{"name": "...", "email": "...", "full_name": "..."}, ...]

    3. Filter operators (comparison, in, between):
       orders = frappe.get_all(
           "Sales Order",
           filters={"docstatus": 1, "grand_total": [">", 10000]},
       )
       filters={"status": ["in", ["Draft", "Submitted"]]}
       filters={"transaction_date": ["between", [from_date, to_date]]}

    Default limit_page_length is 20 - pass 0 for unlimited (use only when
    you know the set is bounded).
  examples:
    good: |
      open_todos = frappe.get_all(
          "ToDo",
          filters={"status": "Open", "allocated_to": frappe.session.user},
          fields=["name", "description", "date"],
      )
    bad: |
      # Don't ask for all fields if you only need one
      names = [r["name"] for r in frappe.get_all("User", filters={"enabled": 1})]
      # Use pluck instead:
      names = frappe.get_all("User", filters={"enabled": 1}, pluck="name")
  pitfalls:
  - Default limit is 20 - set limit_page_length explicitly for predictable behavior
  - fields=None returns only 'name' - pass fields explicitly or use pluck
  - pluck=... is cheaper than extracting from dicts yourself
  related:
  - api_frappe_db_get_value
  - api_frappe_db_sql
  verified_on: '2026-04-16'

api_frappe_get_cached_doc:
  kind: api
  title: frappe.get_cached_doc
  summary: Identical to `frappe.get_doc`, but return from cache if available.
  keywords:
  - cached
  - doc
  - frappe
  - frappe.get_cached_doc
  - get
  - get_cached_doc
  signature: 'frappe.get_cached_doc(*args: Any, **kwargs: Any) -> ''Document'''
  body: Identical to `frappe.get_doc`, but return from cache if available.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_get_doc:
  kind: api
  title: frappe.get_doc
  summary: |
    Load or construct a DocType document. Two call shapes: pass (doctype, name) to load, or pass a dict to construct a new in-memory document.
  signature: frappe.get_doc(doctype_or_dict, name=None) -> Document
  keywords:
  - get_doc
  - load
  - fetch
  - document
  - construct
  - new
  - doc
  - frappe
  body: |
    Primary ways to call:

    1. Load an existing document:
       doc = frappe.get_doc("Sales Order", "SO-0001")

    2. Construct a new in-memory document from a dict (not yet saved):
       doc = frappe.get_doc({
           "doctype": "ToDo",
           "description": "Follow up with customer",
           "allocated_to": "alice@example.com",
       })
       doc.insert()   # <-- save to DB

    3. Construct from kwargs (equivalent to #2 for single-DocType):
       doc = frappe.get_doc(doctype="ToDo", description="...")

    Permission checks run on load (respects user's read permission).
    Raises DoesNotExistError if name doesn't exist. Use frappe.db.exists
    first if the doc might not be there.
  examples:
    good: |
      # Load and modify
      so = frappe.get_doc("Sales Order", doc.sales_order)
      so.priority = "High"
      so.save()
    bad: |
      # Don't: load-then-discard - use frappe.db.get_value for single-field reads
      email = frappe.get_doc("User", user).email   # loads the whole doc
      # Do:
      email = frappe.db.get_value("User", user, "email")
  pitfalls:
  - Loads the FULL document - for a single field, frappe.db.get_value is 10x cheaper
  - Raises DoesNotExistError on missing name; catch or check with frappe.db.exists
    first
  - The dict form doesn't insert - you must call .insert() explicitly
  verified_on: '2026-04-16'

api_frappe_get_list:
  kind: api
  title: frappe.get_list
  summary: List database query via `frappe.model.db_query`. Will also check for permissions.
  keywords:
  - frappe
  - frappe.get_list
  - get
  - get_list
  - list
  signature: frappe.get_list(doctype, *args, **kwargs)
  body: |-
    List database query via `frappe.model.db_query`. Will also check for permissions.

    :param doctype: DocType on which query is to be made.
    :param fields: List of fields or `*`.
    :param filters: List of filters (see example).
    :param order_by: Order By e.g. `creation desc`.
    :param limit_start: Start results at record #. Default 0.
    :param limit_page_length: No of records in the page. Default 20.

    Example usage:

            # simple dict filter
            frappe.get_list("ToDo", fields=["name", "description"], filters = {"owner":"test@example.com"})

            # filter as a list of lists
            frappe.get_list("ToDo", fields="*", filters = [["modified", ">", "2014-01-01"]])
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_get_meta:
  kind: api
  title: frappe.get_meta
  summary: Get metadata for a doctype.
  keywords:
  - frappe
  - frappe.get_meta
  - get
  - get_meta
  - meta
  signature: 'frappe.get_meta(doctype: ''str | DocType'', cached: bool = True) ->
    ''_Meta'''
  body: |-
    Get metadata for a doctype.

    Args:
        doctype: The doctype as a string object.
        cached: Whether to use cached metadata (default: True).

    Returns:
        Meta object for the given doctype.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_has_permission:
  kind: api
  title: frappe.has_permission
  summary: |
    Check if the current user has permission to perform an action on a DocType (or specific document). Returns True/False.
  signature: frappe.has_permission(doctype, ptype='read', doc=None, user=None, throw=False)
    -> bool
  keywords:
  - has_permission
  - permission
  - authorization
  - access
  - check
  body: |
    - ptype: 'read' | 'write' | 'create' | 'delete' | 'submit' | 'cancel'
    - doc: optional docname or Document instance for row-level checks
    - throw=True raises PermissionError instead of returning False
  examples:
    good: |
      if not frappe.has_permission("Sales Order", "write", doc.name):
          frappe.throw(_("Not permitted to edit this order"))
    bad: |
      # Don't bypass - e.g. don't load with ignore_permissions=True
      # just to skip this check unless you're an internal admin tool
      doc = frappe.get_doc("Sales Order", name, ignore_permissions=True)
  pitfalls:
  - Pass the `doc` argument for row-level perms (share / owner rules)
  - throw=True is cleaner than has_permission + manual frappe.throw
  related:
  - server_script_permission_check
  verified_on: '2026-04-16'

api_frappe_integrations_utils_make_delete_request:
  kind: api
  title: frappe.integrations.utils.make_delete_request
  summary: Make a 'DELETE' HTTP request to the given `url` and return processed response.
  keywords:
  - delete
  - frappe
  - frappe.integrations.utils
  - frappe.integrations.utils.make_delete_request
  - integrations
  - make
  - make_delete_request
  - request
  - utils
  signature: 'frappe.integrations.utils.make_delete_request(url: str, **kwargs)'
  body: |-
    Make a 'DELETE' HTTP request to the given `url` and return processed response.

    You can optionally pass the below parameters:

    * `headers`: Headers to be set in the request.
    * `data`: Data to be passed in body of the request.
    * `json`: JSON to be passed in the request.
    * `params`: Query parameters to be passed in the request.
    * `auth`: Auth credentials.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_integrations_utils_make_get_request:
  kind: api
  title: frappe.make_get_request
  summary: |
    Outbound HTTP GET, safe to call from Server Scripts where `import requests` would fail. Returns parsed JSON (dict/list) on success.
  signature: frappe.make_get_request(url, headers=None, auth=None, data=None, **kwargs)
    -> dict | list
  keywords:
  - make_get_request
  - http
  - get
  - request
  - api
  - outbound
  - requests
  body: |
    The Server-Script-safe replacement for `import requests; requests.get(...)`.
    Parses the response as JSON automatically. Raises on non-2xx unless you
    opt out.

    result = frappe.make_get_request(
        "https://api.example.com/v1/widgets",
        headers={"Authorization": "Bearer " + api_key},
    )
  examples:
    good: |
      data = frappe.make_get_request(f"https://rates.example/{currency}")
      doc.fx_rate = data.get("rate")
    bad: |
      # NOT available - `import requests` is forbidden in Server Scripts
      import requests
      requests.get(...)
  pitfalls:
  - Parses JSON automatically - non-JSON responses raise
  - Don't hardcode API keys; use frappe.conf or a Secret DocType
  related:
  - server_script_no_imports
  - api_frappe_integrations_utils_make_post_request
  verified_on: '2026-04-16'

api_frappe_integrations_utils_make_patch_request:
  kind: api
  title: frappe.integrations.utils.make_patch_request
  summary: Make a 'PATCH' HTTP request to the given `url` and return processed response.
  keywords:
  - frappe
  - frappe.integrations.utils
  - frappe.integrations.utils.make_patch_request
  - integrations
  - make
  - make_patch_request
  - patch
  - request
  - utils
  signature: 'frappe.integrations.utils.make_patch_request(url: str, **kwargs)'
  body: |-
    Make a 'PATCH' HTTP request to the given `url` and return processed response.

    You can optionally pass the below parameters:

    * `headers`: Headers to be set in the request.
    * `data`: Data to be passed in body of the request.
    * `json`: JSON to be passed in the request.
    * `params`: Query parameters to be passed in the request.
    * `auth`: Auth credentials.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_integrations_utils_make_post_request:
  kind: api
  title: frappe.make_post_request
  summary: |
    Outbound HTTP POST, safe to call from Server Scripts. Returns parsed JSON. Pass `data` (form) or `data=json.dumps(...)` with a JSON header.
  signature: frappe.make_post_request(url, data=None, headers=None, **kwargs) -> dict
    | list
  keywords:
  - make_post_request
  - http
  - post
  - request
  - api
  - outbound
  - requests
  body: |
    frappe.make_post_request(
        "https://hooks.example.com/notify",
        data=json.dumps({"event": "created", "name": doc.name}),
        headers={"Content-Type": "application/json"},
    )

    For form-encoded, pass `data` as a dict (not stringified).
  examples:
    good: |
      frappe.make_post_request(
          webhook_url,
          data=json.dumps({"doc": doc.name}),
          headers={"Content-Type": "application/json"},
      )
  pitfalls:
  - Use json.dumps(...) when posting JSON - don't pass the dict directly unless form-encoded
  - No `import json` needed in Server Scripts - it's pre-bound
  related:
  - server_script_no_imports
  verified_on: '2026-04-16'

api_frappe_integrations_utils_make_put_request:
  kind: api
  title: frappe.integrations.utils.make_put_request
  summary: Make a 'PUT' HTTP request to the given `url` and return processed response.
  keywords:
  - frappe
  - frappe.integrations.utils
  - frappe.integrations.utils.make_put_request
  - integrations
  - make
  - make_put_request
  - put
  - request
  - utils
  signature: 'frappe.integrations.utils.make_put_request(url: str, **kwargs)'
  body: |-
    Make a 'PUT' HTTP request to the given `url` and return processed response.

    You can optionally pass the below parameters:

    * `headers`: Headers to be set in the request.
    * `data`: Data to be passed in body of the request.
    * `json`: JSON to be passed in the request.
    * `params`: Query parameters to be passed in the request.
    * `auth`: Auth credentials.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_log_error:
  kind: api
  title: frappe.log_error
  summary: |
    Persist an error entry visible under the Error Log DocType. Does NOT raise - the operation continues. Use for background logging that the user shouldn't see.
  signature: frappe.log_error(title=None, message=None, reference_doctype=None, reference_name=None)
  keywords:
  - log_error
  - error
  - log
  - record
  - track
  body: |
    Writes to the "Error Log" DocType. Useful when you want to record
    something unusual without blocking the user:

    try:
        external_call()
    except Exception:
        frappe.log_error(title="External API failed", message=frappe.get_traceback())
  examples:
    good: |
      frappe.log_error(title="Stock sync skipped",
                         message=f"Item {item_code} missing warehouse")
  pitfalls:
  - Silently logs - user sees nothing. Pair with frappe.msgprint if they should know
  verified_on: '2026-04-16'

api_frappe_msgprint:
  kind: api
  title: frappe.msgprint
  summary: |
    Show an informational message to the user (toast / dialog) without raising. Unlike frappe.throw, execution continues.
  signature: frappe.msgprint(msg, title=None, indicator=None, alert=False, ...)
  keywords:
  - msgprint
  - message
  - notify
  - info
  - alert
  - toast
  body: |
    Displays to the user and returns. Commonly used to confirm an async
    action triggered, log context, or give a non-blocking warning.

    Indicator colours: 'red', 'orange', 'green', 'blue'.
    alert=True -> auto-dismissing toast. Omit for a persistent dialog.
  examples:
    good: |
      frappe.msgprint(_("Reminder email queued"), alert=True, indicator="green")
    bad: |
      # Don't use msgprint for validation errors - use frappe.throw
      if doc.total < 0:
          frappe.msgprint("bad total")   # user can still save
  pitfalls:
  - Does NOT stop execution - use frappe.throw for validation blocks
  related:
  - api_frappe_throw
  verified_on: '2026-04-16'

api_frappe_new_doc:
  kind: api
  title: frappe.new_doc
  summary: |
    Shortcut to create an in-memory document of a given DocType, with naming defaults applied. Must call .insert() or .save() to persist.
  signature: 'frappe.new_doc(doctype: str, parent_doc=None, parentfield=None) -> Document'
  keywords:
  - new_doc
  - create
  - document
  - doc
  - frappe
  body: |
    Creates an unsaved document. Equivalent to `frappe.get_doc({"doctype": X})`
    but with auto-naming and default-setter hooks run up-front.

    doc = frappe.new_doc("ToDo")
    doc.description = "Review invoice"
    doc.allocated_to = "alice@example.com"
    doc.insert()

    Use `parent_doc` + `parentfield` for child-table rows when building a
    parent and its children in one flow (rare in Server Scripts).
  examples:
    good: |
      todo = frappe.new_doc("ToDo")
      todo.description = "Follow up"
      todo.insert()
    bad: |
      # Don't skip insert() - the doc never hits the DB
      todo = frappe.new_doc("ToDo")
      todo.description = "Follow up"
      # MISSING: todo.insert()  -> nothing happens
  pitfalls:
  - '.insert() is NOT automatic - forgetting it is the #1 bug with new_doc'
  - Use frappe.get_doc(dict) when you're building from a dict-shaped payload
  verified_on: '2026-04-16'

api_frappe_rename_doc:
  kind: api
  title: frappe.rename_doc
  summary: Renames a doc(dt, old) to doc(dt, new) and updates all linked fields of
    type "Link"
  keywords:
  - doc
  - frappe
  - frappe.rename_doc
  - rename
  - rename_doc
  signature: 'frappe.rename_doc(doctype: str, old: str | int, new: str | int, force:
    bool = False, merge: bool = False, *, ignore_if_exists: bool = False, show_alert:
    bool = True, rebuild_search: bool = True) -> str'
  body: |-
    Renames a doc(dt, old) to doc(dt, new) and updates all linked fields of type "Link"

    Calls `frappe.model.rename_doc.rename_doc`
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_render_template:
  kind: api
  title: frappe.render_template
  summary: Render a template using Jinja
  keywords:
  - frappe
  - frappe.render_template
  - render
  - render_template
  - template
  signature: frappe.render_template(template, context=None, is_path=None, safe_render=True)
  body: |-
    Render a template using Jinja

    :param template: path or HTML containing the jinja template
    :param context: dict of properties to pass to the template
    :param is_path: (optional) assert that the `template` parameter is a path
    :param safe_render: (optional) prevent server side scripting via jinja templating
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_sendmail:
  kind: api
  title: frappe.sendmail
  summary: |
    Queue an email. For most use cases, the Notification DocType is a better fit - declarative, admin-visible, and doesn't need a Server Script. Use sendmail() only when the email is purely transactional.
  signature: frappe.sendmail(recipients, subject='', message='', sender=None, reference_doctype=None,
    reference_name=None, ...)
  keywords:
  - sendmail
  - email
  - notify
  - send
  - mail
  body: |
    frappe.sendmail(
        recipients=["alice@example.com"],
        subject="Your order shipped",
        message="<p>Tracking: ABC123</p>",
        reference_doctype="Sales Order",
        reference_name=doc.name,
    )

    Queued via the email queue - not sent synchronously.
  examples:
    bad: |
      # Don't re-implement Notification in a Server Script
      # Build a Notification DocType instead (declarative, admin-visible)
      frappe.sendmail(
          recipients=[...],
          subject="Order submitted",
          message="...",
      )
  pitfalls:
  - For 'send email when X happens', prefer a Notification DocType
  - Emails are QUEUED; failures surface in the Email Queue, not synchronously
  related:
  - notification_doctype_vs_server_script
  verified_on: '2026-04-16'

api_frappe_throw:
  kind: api
  title: frappe.throw
  summary: |
    Raise a user-facing validation error that stops the current operation and displays the message in the UI. Use in validation scripts instead of raw `raise ValidationError`.
  signature: frappe.throw(msg, exc=ValidationError, title=None)
  keywords:
  - throw
  - validation
  - error
  - block
  - prevent
  - reject
  - stop
  body: |
    Raises a ValidationError which Frappe intercepts and turns into a
    toast/modal on the frontend. The transaction is rolled back.

    For translatable messages, wrap the string with `_()`:
       frappe.throw(_("Date of birth cannot be in the future"))

    Pass a specific exc class for different handling (e.g. PermissionError):
       frappe.throw(
           _("You cannot edit closed invoices"),
           exc=frappe.PermissionError,
       )
  examples:
    good: |
      if doc.grand_total < 0:
          frappe.throw(_("Grand total cannot be negative"))
    bad: |
      # Don't raise bare exceptions - the user just sees "Internal error"
      if doc.grand_total < 0:
          raise ValueError("bad total")
  pitfalls:
  - Prefer frappe.throw(_(msg)) over raise so translations work
  - Don't concatenate user input into the message - use Jinja-style formatting carefully
  related:
  - api_frappe_msgprint
  - server_script_no_imports
  verified_on: '2026-04-16'

api_frappe_utils_add_days:
  kind: api
  title: frappe.utils.add_days
  summary: |
    Add (or subtract with negative n) N days from a date. Returns an ISO date string.
  signature: frappe.utils.add_days(date, days) -> str
  keywords:
  - add_days
  - shift
  - offset
  - date
  - arithmetic
  body: |
    due = frappe.utils.add_days(frappe.utils.nowdate(), 7)     # +1 week
    last_week = frappe.utils.add_days(frappe.utils.nowdate(), -7)  # -1 week
  examples:
    good: |
      doc.due_date = frappe.utils.add_days(doc.transaction_date, 30)
  pitfalls:
  - Returns a STRING (ISO date) not a date object
  verified_on: '2026-04-16'

api_frappe_utils_add_months:
  kind: api
  title: frappe.utils.add_months
  summary: Return a new date after adding the given number of `months` to the given
    `date`.
  keywords:
  - add
  - add_months
  - frappe
  - frappe.utils
  - frappe.utils.add_months
  - months
  - utils
  signature: 'frappe.utils.add_months(date: str | datetime.date | datetime.datetime,
    months: int | float) -> str | datetime.date | datetime.datetime'
  body: Return a new date after adding the given number of `months` to the given `date`.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_add_to_date:
  kind: api
  title: frappe.utils.add_to_date
  summary: Adds `days` to the given date
  keywords:
  - add
  - add_to_date
  - date
  - frappe
  - frappe.utils
  - frappe.utils.add_to_date
  - utils
  signature: 'frappe.utils.add_to_date(date: str | datetime.date | datetime.datetime
    | None = None, years=0, months=0, weeks=0, days=0, hours=0, minutes=0, seconds=0,
    as_string=False, as_datetime=False) -> str | datetime.date | datetime.datetime'
  body: Adds `days` to the given date
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_add_years:
  kind: api
  title: frappe.utils.add_years
  summary: Return a new date after adding the given number of `years` to the given
    `date`.
  keywords:
  - add
  - add_years
  - frappe
  - frappe.utils
  - frappe.utils.add_years
  - utils
  - years
  signature: 'frappe.utils.add_years(date: str | datetime.date | datetime.datetime,
    years: int | float) -> str | datetime.date | datetime.datetime'
  body: Return a new date after adding the given number of `years` to the given `date`.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_ceil:
  kind: api
  title: frappe.utils.ceil
  summary: Return the smallest integer greater than or equal to the given number.
  keywords:
  - ceil
  - frappe
  - frappe.utils
  - frappe.utils.ceil
  - utils
  signature: 'frappe.utils.ceil(s: int | float | str) -> int'
  body: Return the smallest integer greater than or equal to the given number.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_cint:
  kind: api
  title: frappe.utils.cint
  summary: |
    Safe int conversion. Returns 0 for None / empty / unparseable input. Int flavour of flt() - same safety, same caveats.
  signature: frappe.utils.cint(value, default=0) -> int
  keywords:
  - cint
  - int
  - integer
  - number
  - convert
  - parse
  - safe
  body: |
    n = frappe.utils.cint(doc.qty)      # None -> 0
    n = frappe.utils.cint("5", 99)      # 5
    n = frappe.utils.cint("", 99)       # 99
  related:
  - api_frappe_utils_flt
  verified_on: '2026-04-16'

api_frappe_utils_comma_and:
  kind: api
  title: frappe.utils.comma_and
  summary: Return the given list or tuple as a comma separated string with the last
    item joined by 'and'. e.g. ['a', 'b', 'c'] -> 'a, b and c'
  keywords:
  - and
  - comma
  - comma_and
  - frappe
  - frappe.utils
  - frappe.utils.comma_and
  - utils
  signature: 'frappe.utils.comma_and(some_list: list | tuple, add_quotes=True) ->
    str'
  body: |-
    Return the given list or tuple as a comma separated string with the last item joined by 'and'.
    e.g. ['a', 'b', 'c'] -> 'a, b and c'

    If `add_quotes` is True, each item in the list will be wrapped in single quotes.
    e.g. ['a', 'b', 'c'] -> "'a', 'b' and 'c'"
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_comma_or:
  kind: api
  title: frappe.utils.comma_or
  summary: Return the given list or tuple as a comma separated string with the last
    item joined by 'or'. e.g. ['a', 'b', 'c'] -> 'a, b or c'
  keywords:
  - comma
  - comma_or
  - frappe
  - frappe.utils
  - frappe.utils.comma_or
  - utils
  signature: 'frappe.utils.comma_or(some_list: list | tuple, add_quotes=True) -> str'
  body: |-
    Return the given list or tuple as a comma separated string with the last item joined by 'or'.
    e.g. ['a', 'b', 'c'] -> 'a, b or c'

    If `add_quotes` is True, each item in the list will be wrapped in single quotes.
    e.g. ['a', 'b', 'c'] -> "'a', 'b' or 'c'"
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_comma_sep:
  kind: api
  title: frappe.utils.comma_sep
  summary: Return the given list or tuple as a comma separated string, with the last
    item joined by the given string format pattern.
  keywords:
  - comma
  - comma_sep
  - frappe
  - frappe.utils
  - frappe.utils.comma_sep
  - sep
  - utils
  signature: 'frappe.utils.comma_sep(some_list: list | tuple, pattern: str, add_quotes=True)
    -> str'
  body: |-
    Return the given list or tuple as a comma separated string, with the last item joined by the given string format pattern.

    If `add_quotes` is True, each item in the list will be wrapped in single quotes.

    e.g. if `some_list` is ['a', 'b', 'c'] and `pattern` is '{0} or {1}', the output will be 'a, b or c'
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_compare:
  kind: api
  title: frappe.utils.compare
  summary: Compare two values using the specified operator with optional fieldtype
    casting.
  keywords:
  - compare
  - frappe
  - frappe.utils
  - frappe.utils.compare
  - utils
  signature: 'frappe.utils.compare(val1: Any, condition: str, val2: Any, fieldtype:
    str | None = None) -> bool'
  body: |-
    Compare two values using the specified operator with optional fieldtype casting.

    Args:
            val1: The left operand value to compare
            condition: The comparison operator (e.g., "=", ">", "is", "in", "like")
            val2: The right operand value to compare against
            fieldtype: Optional fieldtype for casting val1 (and val2 for most operators)

    Returns:
            bool: True if the comparison evaluates to True, False otherwise

    Note:
    - For "is" operator: No casting is performed to preserve None values
    - For "in"/"not in" operators: Only val1 is cast (if not None), val2 remains unchanged
    - For "Timespan" operator: No casting is performed
    - For other operators: Both val1 and val2 are cast to the specified fieldtype
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_convert_utc_to_system_timezone:
  kind: api
  title: frappe.utils.convert_utc_to_system_timezone
  summary: Return the given UTC `datetime` timestamp in system timezone.
  keywords:
  - convert
  - convert_utc_to_system_timezone
  - frappe
  - frappe.utils
  - frappe.utils.convert_utc_to_system_timezone
  - system
  - timezone
  - utc
  - utils
  signature: 'frappe.utils.convert_utc_to_system_timezone(utc_timestamp: datetime.datetime)
    -> datetime.datetime'
  body: Return the given UTC `datetime` timestamp in system timezone.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_cstr:
  kind: api
  title: frappe.utils.cstr
  summary: Convert the given argument to string.
  keywords:
  - cstr
  - frappe
  - frappe.utils
  - frappe.utils.cstr
  - utils
  signature: frappe.utils.cstr(s, encoding='utf-8') -> str
  body: Convert the given argument to string.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_date_diff:
  kind: api
  title: frappe.utils.date_diff
  summary: |
    Difference between two dates in DAYS (an int). date_diff(end, start) - mind the argument order.
  signature: frappe.utils.date_diff(string_ed_date, string_st_date) -> int
  keywords:
  - date_diff
  - diff
  - days
  - age
  - between
  - difference
  body: |
    days = frappe.utils.date_diff(frappe.utils.nowdate(), doc.start_date)
    # -> e.g. 120 (positive if end > start)

    Accepts strings or date objects. Returns an int (days).
  examples:
    good: |
      # Minimum age check
      age_days = frappe.utils.date_diff(frappe.utils.nowdate(), doc.dob)
      if age_days < 18 * 365:
          frappe.throw("Must be 18 or older")
    bad: |
      # Argument order matters - this gives a NEGATIVE result
      # when the user's dob is in the past (which is always)
      age_days = frappe.utils.date_diff(doc.dob, frappe.utils.nowdate())
  pitfalls:
  - 'Argument order: (end, start). Swapping gives the NEGATIVE value'
  - Returns DAYS, not seconds - use time_diff_in_seconds for sub-day precision
  related:
  - api_frappe_utils_nowdate
  - api_frappe_utils_getdate
  verified_on: '2026-04-16'

api_frappe_utils_encode:
  kind: api
  title: frappe.utils.encode
  summary: (Auto-scraped from frappe.utils; no upstream docstring.)
  keywords:
  - encode
  - frappe
  - frappe.utils
  - frappe.utils.encode
  - utils
  signature: frappe.utils.encode(obj, encoding='utf-8')
  body: (Auto-scraped from frappe.utils; no upstream docstring.)
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_escape_html:
  kind: api
  title: frappe.utils.escape_html
  summary: Return the given text with HTML special characters escaped.
  keywords:
  - escape
  - escape_html
  - frappe
  - frappe.utils
  - frappe.utils.escape_html
  - html
  - utils
  signature: 'frappe.utils.escape_html(text: str) -> str'
  body: |-
    Return the given text with HTML special characters escaped.

    e.g. '<h1>Hello</h1>' -> '&lt;h1&gt;Hello&lt;/h1&gt;'
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_evaluate_filters:
  kind: api
  title: frappe.utils.evaluate_filters
  summary: Return True if doc matches filters.
  keywords:
  - evaluate
  - evaluate_filters
  - filters
  - frappe
  - frappe.utils
  - frappe.utils.evaluate_filters
  - utils
  signature: 'frappe.utils.evaluate_filters(doc: ''Mapping'', filters: FilterSignature)'
  body: Return True if doc matches filters.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_expand_relative_urls:
  kind: api
  title: frappe.utils.expand_relative_urls
  summary: Expand relative urls in the given `html`.
  keywords:
  - expand
  - expand_relative_urls
  - frappe
  - frappe.utils
  - frappe.utils.expand_relative_urls
  - relative
  - urls
  - utils
  signature: 'frappe.utils.expand_relative_urls(html: str) -> str'
  body: |-
    Expand relative urls in the given `html`.

    e.g. If HTML is '<a href="/files/abc.jpeg">View Image</a>' and site URL is 'https://frappe.io',
            returns '<a href="https://frappe.io/files/abc.jpeg">View Image</a>'.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_filter_strip_join:
  kind: api
  title: frappe.utils.filter_strip_join
  summary: given a list, filter None values, strip spaces and join
  keywords:
  - filter
  - filter_strip_join
  - frappe
  - frappe.utils
  - frappe.utils.filter_strip_join
  - join
  - strip
  - utils
  signature: 'frappe.utils.filter_strip_join(some_list: list[str], sep: str) -> list[str]'
  body: given a list, filter None values, strip spaces and join
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_floor:
  kind: api
  title: frappe.utils.floor
  summary: Return a number representing the largest integer less than or equal to
    the specified number.
  keywords:
  - floor
  - frappe
  - frappe.utils
  - frappe.utils.floor
  - utils
  signature: 'frappe.utils.floor(s: int | float | str) -> int'
  body: Return a number representing the largest integer less than or equal to the
    specified number.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_flt:
  kind: api
  title: frappe.utils.flt
  summary: |
    Safe float conversion. Returns 0.0 for None / empty string / unparseable input instead of raising. Use whenever you're about to do arithmetic on a field that might be None.
  signature: frappe.utils.flt(value, precision=None) -> float
  keywords:
  - flt
  - float
  - number
  - convert
  - parse
  - safe
  body: |
    x = frappe.utils.flt(doc.discount_percent)   # None -> 0.0, "12.5" -> 12.5
    x = frappe.utils.flt(value, precision=2)     # rounded to 2 decimals

    Common pattern: defend against None-ish fields before math.
  examples:
    good: |
      total = frappe.utils.flt(doc.qty) * frappe.utils.flt(doc.rate)
    bad: |
      # Don't - doc.qty might be None -> TypeError
      total = doc.qty * doc.rate
  pitfalls:
  - Returns 0.0 for ANYTHING unparseable - won't tell you the source was bad
  related:
  - api_frappe_utils_cint
  verified_on: '2026-04-16'

api_frappe_utils_fmt_money:
  kind: api
  title: frappe.utils.fmt_money
  summary: Convert to string with commas for thousands, millions etc.
  keywords:
  - fmt
  - fmt_money
  - frappe
  - frappe.utils
  - frappe.utils.fmt_money
  - money
  - utils
  signature: 'frappe.utils.fmt_money(amount: str | float | int | None, precision:
    int | None = None, currency: str | None = None, format: str | None = None) ->
    str'
  body: Convert to string with commas for thousands, millions etc.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_format_date:
  kind: api
  title: frappe.utils.format_date
  summary: Convert the given string date to :data:`user_date_format`.
  keywords:
  - date
  - format
  - format_date
  - frappe
  - frappe.utils
  - frappe.utils.format_date
  - utils
  signature: 'frappe.utils.format_date(string_date=None, format_string: str | None
    = None, parse_day_first: bool = False) -> str'
  body: |-
    Convert the given string date to :data:`user_date_format`.

    User format specified in defaults

    Examples:

    * dd-mm-yyyy
    * mm-dd-yyyy
    * dd/mm/yyyy
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_format_datetime:
  kind: api
  title: frappe.utils.format_datetime
  summary: Convert the given string time to :data:`user_datetime_format` User format
    specified in defaults
  keywords:
  - datetime
  - format
  - format_datetime
  - frappe
  - frappe.utils
  - frappe.utils.format_datetime
  - utils
  signature: 'frappe.utils.format_datetime(datetime_string: str | datetime.date |
    datetime.datetime, format_string: str | None = None) -> str'
  body: |-
    Convert the given string time to :data:`user_datetime_format`
    User format specified in defaults

    Examples:

    * dd-mm-yyyy HH:mm:ss
    * mm-dd-yyyy HH:mm
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_format_duration:
  kind: api
  title: frappe.utils.format_duration
  summary: Convert the given duration value in seconds to duration format.
  keywords:
  - duration
  - format
  - format_duration
  - frappe
  - frappe.utils
  - frappe.utils.format_duration
  - utils
  signature: 'frappe.utils.format_duration(seconds: float | int, hide_days: bool =
    False) -> str'
  body: |-
    Convert the given duration value in seconds to duration format.

    example:
    convert 12885 to '3h 34m 45s' where 12885 = seconds in float
            -12885 to '-3h 34m 45s'
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_format_time:
  kind: api
  title: frappe.utils.format_time
  summary: Convert the given string time to :data:`user_time_format`.
  keywords:
  - format
  - format_time
  - frappe
  - frappe.utils
  - frappe.utils.format_time
  - time
  - utils
  signature: 'frappe.utils.format_time(time_string=None, format_string: str | None
    = None) -> str'
  body: |-
    Convert the given string time to :data:`user_time_format`.

    User format specified in defaults

    Examples:

    * HH:mm:ss
    * HH:mm
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_formatdate:
  kind: api
  title: frappe.utils.formatdate
  summary: Convert the given string date to :data:`user_date_format`.
  keywords:
  - formatdate
  - frappe
  - frappe.utils
  - frappe.utils.formatdate
  - utils
  signature: 'frappe.utils.formatdate(string_date=None, format_string: str | None
    = None, parse_day_first: bool = False) -> str'
  body: |-
    Convert the given string date to :data:`user_date_format`.

    User format specified in defaults

    Examples:

    * dd-mm-yyyy
    * mm-dd-yyyy
    * dd/mm/yyyy
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_generate_hash:
  kind: api
  title: frappe.utils.generate_hash
  summary: Generates a random hash using best available randomness source and returns
    it.
  keywords:
  - frappe
  - frappe.utils
  - frappe.utils.generate_hash
  - generate
  - generate_hash
  - hash
  - utils
  signature: frappe.utils.generate_hash(*args, **kwargs) -> str
  body: |-
    Generates a random hash using best available randomness source and returns it.

    You can optionally provide the `length` of the hash to be generated. Default is 56.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_get_abbr:
  kind: api
  title: frappe.utils.get_abbr
  summary: Return the abbreviation of the given string.
  keywords:
  - abbr
  - frappe
  - frappe.utils
  - frappe.utils.get_abbr
  - get
  - get_abbr
  - utils
  signature: 'frappe.utils.get_abbr(string: str, max_len: int = 2) -> str'
  body: |-
    Return the abbreviation of the given string.

    Examples:

    * "John Doe" => "JD"
    * "Jenny Jane Doe" => "JJ" (default, `max_len` = 2)
    * "Jenny Jane Doe" => "JJD" (`max_len` = 3)

    Return "?" if the given string is empty.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_get_absolute_url:
  kind: api
  title: frappe.utils.get_absolute_url
  summary: Return the absolute route for the form view of the given document in the
    desk.
  keywords:
  - absolute
  - frappe
  - frappe.utils
  - frappe.utils.get_absolute_url
  - get
  - get_absolute_url
  - url
  - utils
  signature: 'frappe.utils.get_absolute_url(doctype: str, name: str) -> str'
  body: |-
    Return the absolute route for the form view of the given document in the desk.

    e.g. when doctype="Sales Invoice" and name="INV-00001", returns '/desk/sales-invoice/INV-00001'
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_get_date_str:
  kind: api
  title: frappe.utils.get_date_str
  summary: Return the given datetime like object (datetime.date, datetime.datetime,
    string) as a string in `yyyy-mm-dd` format.
  keywords:
  - date
  - frappe
  - frappe.utils
  - frappe.utils.get_date_str
  - get
  - get_date_str
  - str
  - utils
  signature: 'frappe.utils.get_date_str(date_obj: str | datetime.date | datetime.datetime)
    -> str'
  body: Return the given datetime like object (datetime.date, datetime.datetime, string)
    as a string in `yyyy-mm-dd` format.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_get_datetime:
  kind: api
  title: frappe.utils.get_datetime
  summary: 'Return the below mentioned values based on the given `datetime_str`:'
  keywords:
  - datetime
  - frappe
  - frappe.utils
  - frappe.utils.get_datetime
  - get
  - get_datetime
  - utils
  signature: 'frappe.utils.get_datetime(datetime_str: str | datetime.date | datetime.datetime
    | None | tuple | list = None) -> datetime.datetime | None'
  body: |-
    Return the below mentioned values based on the given `datetime_str`:

    * If `datetime_str` is None, returns datetime object of current datetime
    * If `datetime_str` is already a datetime object, returns the same
    * If `datetime_str` is a timedelta object, returns the same
    * If `datetime_str` is a list or tuple, returns a datetime object
    * If `datetime_str` is a date object, returns a datetime object
    * If `datetime_str` is a valid date string, returns a datetime object for the same
    * If `datetime_str` is an invalid date string, returns None
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_get_datetime_in_timezone:
  kind: api
  title: frappe.utils.get_datetime_in_timezone
  summary: Return the current datetime in the given timezone (e.g. 'Asia/Kolkata').
  keywords:
  - datetime
  - frappe
  - frappe.utils
  - frappe.utils.get_datetime_in_timezone
  - get
  - get_datetime_in_timezone
  - timezone
  - utils
  signature: 'frappe.utils.get_datetime_in_timezone(time_zone: str) -> datetime.datetime'
  body: Return the current datetime in the given timezone (e.g. 'Asia/Kolkata').
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_get_datetime_str:
  kind: api
  title: frappe.utils.get_datetime_str
  summary: Return the given datetime like object (datetime.date, datetime.datetime,
    string) as a string in `yyyy-mm-dd hh:mm:ss` format.
  keywords:
  - datetime
  - frappe
  - frappe.utils
  - frappe.utils.get_datetime_str
  - get
  - get_datetime_str
  - str
  - utils
  signature: 'frappe.utils.get_datetime_str(datetime_obj: str | datetime.date | datetime.datetime)
    -> str'
  body: Return the given datetime like object (datetime.date, datetime.datetime, string)
    as a string in `yyyy-mm-dd hh:mm:ss` format.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_get_eta:
  kind: api
  title: frappe.utils.get_eta
  summary: (Auto-scraped from frappe.utils; no upstream docstring.)
  keywords:
  - eta
  - frappe
  - frappe.utils
  - frappe.utils.get_eta
  - get
  - get_eta
  - utils
  signature: 'frappe.utils.get_eta(from_time: str | datetime.date | datetime.datetime,
    percent_complete) -> str'
  body: (Auto-scraped from frappe.utils; no upstream docstring.)
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_get_filter:
  kind: api
  title: frappe.utils.get_filter
  summary: 'Return a `_dict` like:'
  keywords:
  - filter
  - frappe
  - frappe.utils
  - frappe.utils.get_filter
  - get
  - get_filter
  - utils
  signature: 'frappe.utils.get_filter(doctype: str, filters: FilterSignature, filters_config=None)
    -> ''frappe._dict'''
  body: |-
    Return a `_dict` like:

    {
            "doctype": ...
            "fieldname": ...
            "operator": ...
            "value": ...
            "fieldtype": ...
    }
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_get_first_day:
  kind: api
  title: frappe.utils.get_first_day
  summary: Return the first day of the month for the date specified by date object.
  keywords:
  - day
  - first
  - frappe
  - frappe.utils
  - frappe.utils.get_first_day
  - get
  - get_first_day
  - utils
  signature: 'frappe.utils.get_first_day(dt, d_years: int = 0, d_months: int = 0,
    as_str: bool = False) -> str | datetime.date'
  body: |-
    Return the first day of the month for the date specified by date object.

    Also, add `d_years` and `d_months` if specified.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_get_first_day_of_week:
  kind: api
  title: frappe.utils.get_first_day_of_week
  summary: Return the first day of the week (as per System Settings or Sunday by default)
    for the given datetime like object (`dt`).
  keywords:
  - day
  - first
  - frappe
  - frappe.utils
  - frappe.utils.get_first_day_of_week
  - get
  - get_first_day_of_week
  - utils
  - week
  signature: 'frappe.utils.get_first_day_of_week(dt: str | datetime.date | datetime.datetime,
    as_str=False) -> datetime.date | str'
  body: |-
    Return the first day of the week (as per System Settings or Sunday by default) for the given datetime like object (`dt`).

    If `as_str` is True, the first day of the week is returned as a string in `yyyy-mm-dd` format.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_get_host_name:
  kind: api
  title: frappe.utils.get_host_name
  summary: Return the hostname of the current site.
  keywords:
  - frappe
  - frappe.utils
  - frappe.utils.get_host_name
  - get
  - get_host_name
  - host
  - name
  - utils
  signature: frappe.utils.get_host_name() -> str
  body: |-
    Return the hostname of the current site.

    e.g. If site is 'https://cloud.frappe.io', returns 'cloud.frappe.io'.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_get_host_name_from_request:
  kind: api
  title: frappe.utils.get_host_name_from_request
  summary: Return the hostname (`request.host`) from the request headers.
  keywords:
  - frappe
  - frappe.utils
  - frappe.utils.get_host_name_from_request
  - from
  - get
  - get_host_name_from_request
  - host
  - name
  - request
  - utils
  signature: frappe.utils.get_host_name_from_request() -> str
  body: Return the hostname (`request.host`) from the request headers.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_get_last_day:
  kind: api
  title: frappe.utils.get_last_day
  summary: 'Return last day of the month using:'
  keywords:
  - day
  - frappe
  - frappe.utils
  - frappe.utils.get_last_day
  - get
  - get_last_day
  - last
  - utils
  signature: frappe.utils.get_last_day(dt)
  body: |-
    Return last day of the month using:

    `get_first_day(dt, 0, 1) + datetime.timedelta(-1)`
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_get_last_day_of_week:
  kind: api
  title: frappe.utils.get_last_day_of_week
  summary: Return the last day of the week (first day is taken from System Settings
    or Sunday by default) for the given datetime like object (`dt`).
  keywords:
  - day
  - frappe
  - frappe.utils
  - frappe.utils.get_last_day_of_week
  - get
  - get_last_day_of_week
  - last
  - utils
  - week
  signature: 'frappe.utils.get_last_day_of_week(dt: str | datetime.date | datetime.datetime,
    as_str=False) -> datetime.date | str'
  body: |-
    Return the last day of the week (first day is taken from System Settings or Sunday by default) for the given datetime like object (`dt`).

    If `as_str` is True, the last day of the week is returned as a string in `yyyy-mm-dd` format.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_get_link_to_form:
  kind: api
  title: frappe.utils.get_link_to_form
  summary: Return the HTML link to the given document's form view.
  keywords:
  - form
  - frappe
  - frappe.utils
  - frappe.utils.get_link_to_form
  - get
  - get_link_to_form
  - link
  - utils
  signature: 'frappe.utils.get_link_to_form(doctype: str, name: str | None = None,
    label: str | None = None) -> str'
  body: |-
    Return the HTML link to the given document's form view.

    e.g. get_link_to_form("Sales Invoice", "INV-0001", "Link Label") returns:
        '<a href="https://frappe.io/desk/sales-invoice/INV-0001">Link Label</a>'.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_get_link_to_report:
  kind: api
  title: frappe.utils.get_link_to_report
  summary: Return the HTML link to the given report.
  keywords:
  - frappe
  - frappe.utils
  - frappe.utils.get_link_to_report
  - get
  - get_link_to_report
  - link
  - report
  - utils
  signature: 'frappe.utils.get_link_to_report(name: str, label: str | None = None,
    report_type: str | None = None, doctype: str | None = None, filters: dict | None
    = None) -> str'
  body: |-
    Return the HTML link to the given report.

    e.g. get_link_to_report("Revenue Report", "Link Label") returns:
            '<a href="https://frappe.io/desk/query-report/Revenue%20Report">Link Label</a>'.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_get_month:
  kind: api
  title: frappe.utils.get_month
  summary: Return the month name (e.g. 'January') for the given datetime like object
    (datetime.date, datetime.datetime, string).
  keywords:
  - frappe
  - frappe.utils
  - frappe.utils.get_month
  - get
  - get_month
  - month
  - utils
  signature: 'frappe.utils.get_month(datetime: str | datetime.date | datetime.datetime
    | None = None) -> str'
  body: |-
    Return the month name (e.g. 'January') for the given datetime like object (datetime.date, datetime.datetime, string).

    If `datetime` argument is not provided, the current month name is returned.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_get_number_format_info:
  kind: api
  title: frappe.utils.get_number_format_info
  summary: 'DEPRECATED: use `NumberFormat.from_string()` from `frappe.utils.number_format`
    instead.'
  keywords:
  - format
  - frappe
  - frappe.utils
  - frappe.utils.get_number_format_info
  - get
  - get_number_format_info
  - info
  - number
  - utils
  signature: 'frappe.utils.get_number_format_info(format: str) -> tuple[str, str,
    int]'
  body: |-
    DEPRECATED: use `NumberFormat.from_string()` from `frappe.utils.number_format` instead.

    Return the decimal separator, thousands separator and precision for the given number `format` string.

    e.g. get_number_format_info('#,##,###.##') -> ('.', ',', 2)

    Will return ('.', ',', 2) for format strings which can't be guessed.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_get_quarter_ending:
  kind: api
  title: frappe.utils.get_quarter_ending
  summary: Return the end date of the quarter for the given datetime like object (`date`).
  keywords:
  - ending
  - frappe
  - frappe.utils
  - frappe.utils.get_quarter_ending
  - get
  - get_quarter_ending
  - quarter
  - utils
  signature: 'frappe.utils.get_quarter_ending(date: str | datetime.date | datetime.datetime
    | None = None, as_str=False) -> str | datetime.date'
  body: |-
    Return the end date of the quarter for the given datetime like object (`date`).

    If `date` is None, the current quarter end date is returned.
    If `as_str` is True, the end date of the quarter is returned as a string in `yyyy-mm-dd` format.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_get_quarter_start:
  kind: api
  title: frappe.utils.get_quarter_start
  summary: Return the start date of the quarter for the given datetime like object
    (`dt`).
  keywords:
  - frappe
  - frappe.utils
  - frappe.utils.get_quarter_start
  - get
  - get_quarter_start
  - quarter
  - start
  - utils
  signature: 'frappe.utils.get_quarter_start(dt: str | datetime.date | datetime.datetime
    | None = None, as_str: bool = False) -> str | datetime.date'
  body: |-
    Return the start date of the quarter for the given datetime like object (`dt`).

    If `dt` is None, the current quarter start date is returned.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_get_system_timezone:
  kind: api
  title: frappe.utils.get_system_timezone
  summary: Return the system timezone.
  keywords:
  - frappe
  - frappe.utils
  - frappe.utils.get_system_timezone
  - get
  - get_system_timezone
  - system
  - timezone
  - utils
  signature: frappe.utils.get_system_timezone() -> str
  body: Return the system timezone.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_get_time:
  kind: api
  title: frappe.utils.get_time
  summary: Return a `datetime.time` object for the given `time_str`.
  keywords:
  - frappe
  - frappe.utils
  - frappe.utils.get_time
  - get
  - get_time
  - time
  - utils
  signature: 'frappe.utils.get_time(time_str: str | datetime.datetime | datetime.time
    | datetime.timedelta) -> datetime.time'
  body: |-
    Return a `datetime.time` object for the given `time_str`.

    If the given argument is already a `datetime.time` object, it is returned as is.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_get_time_str:
  kind: api
  title: frappe.utils.get_time_str
  summary: Return the given timedelta object as a string in `hh:mm:ss` format.
  keywords:
  - frappe
  - frappe.utils
  - frappe.utils.get_time_str
  - get
  - get_time_str
  - str
  - time
  - utils
  signature: 'frappe.utils.get_time_str(timedelta_obj: datetime.timedelta | str) ->
    str'
  body: Return the given timedelta object as a string in `hh:mm:ss` format.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_get_timedelta:
  kind: api
  title: frappe.utils.get_timedelta
  summary: Return `datetime.timedelta` object from string value of a valid time format.
  keywords:
  - frappe
  - frappe.utils
  - frappe.utils.get_timedelta
  - get
  - get_timedelta
  - timedelta
  - utils
  signature: 'frappe.utils.get_timedelta(time: str | datetime.timedelta | None = None)
    -> datetime.timedelta | None'
  body: |-
    Return `datetime.timedelta` object from string value of a valid time format.

    Return None if `time` is not a valid format.

    Args:
            time (str | datetime.timedelta): A valid time representation. This string is parsed
            using `dateutil.parser.parse`. Examples of valid inputs are:
            '0:0:0', '17:21:00', '2012-01-19 17:21:00'. Checkout
            https://dateutil.readthedocs.io/en/stable/parser.html#dateutil.parser.parse

    Return:
            datetime.timedelta: Timedelta object equivalent of the passed `time` string
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_get_timespan_date_range:
  kind: api
  title: frappe.utils.get_timespan_date_range
  summary: Return the date range (start_date, end_date) tuple for the given timespan.
  keywords:
  - date
  - frappe
  - frappe.utils
  - frappe.utils.get_timespan_date_range
  - get
  - get_timespan_date_range
  - range
  - timespan
  - utils
  signature: 'frappe.utils.get_timespan_date_range(timespan: Literal[''last week'',
    ''last month'', ''last quarter'', ''last 6 months'', ''last year'', ''yesterday'',
    ''today'', ''tomorrow'', ''this week'', ''this month'', ''this quarter'', ''this
    year'', ''next week'', ''next month'', ''next quarter'', ''next 6 months'', ''next
    year'']) -> tuple[datetime.datetime, datetime.datetime] | None'
  body: Return the date range (start_date, end_date) tuple for the given timespan.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_get_timestamp:
  kind: api
  title: frappe.utils.get_timestamp
  summary: Return the Unix timestamp (seconds since Epoch) for the given `date`. If
    `date` is None, the current timestamp is returned.
  keywords:
  - frappe
  - frappe.utils
  - frappe.utils.get_timestamp
  - get
  - get_timestamp
  - timestamp
  - utils
  signature: 'frappe.utils.get_timestamp(date: str | datetime.date | datetime.datetime
    | None = None) -> float'
  body: |-
    Return the Unix timestamp (seconds since Epoch) for the given `date`.
    If `date` is None, the current timestamp is returned.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_get_url:
  kind: api
  title: frappe.utils.get_url
  summary: Get app url from request.
  keywords:
  - frappe
  - frappe.utils
  - frappe.utils.get_url
  - get
  - get_url
  - url
  - utils
  signature: 'frappe.utils.get_url(uri: str | None = None, full_address: bool = False,
    allow_header_override: bool = True) -> str'
  body: Get app url from request.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_get_url_to_form:
  kind: api
  title: frappe.utils.get_url_to_form
  summary: Return the absolute URL for the form view of the given document in the
    desk.
  keywords:
  - form
  - frappe
  - frappe.utils
  - frappe.utils.get_url_to_form
  - get
  - get_url_to_form
  - url
  - utils
  signature: 'frappe.utils.get_url_to_form(doctype: str, name: str | None = None)
    -> str'
  body: |-
    Return the absolute URL for the form view of the given document in the desk.

    e.g. when doctype="Sales Invoice" and your site URL is "https://frappe.io",
             returns 'https://frappe.io/desk/sales-invoice/INV-00001'
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_get_url_to_list:
  kind: api
  title: frappe.utils.get_url_to_list
  summary: Return the absolute URL for the list view of the given document in the
    desk.
  keywords:
  - frappe
  - frappe.utils
  - frappe.utils.get_url_to_list
  - get
  - get_url_to_list
  - list
  - url
  - utils
  signature: 'frappe.utils.get_url_to_list(doctype: str) -> str'
  body: |-
    Return the absolute URL for the list view of the given document in the desk.

    e.g. when doctype="Sales Invoice" and your site URL is "https://frappe.io",
             returns 'https://frappe.io/desk/sales-invoice'
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_get_url_to_report:
  kind: api
  title: frappe.utils.get_url_to_report
  summary: Return the absolute URL for the report in the desk.
  keywords:
  - frappe
  - frappe.utils
  - frappe.utils.get_url_to_report
  - get
  - get_url_to_report
  - report
  - url
  - utils
  signature: 'frappe.utils.get_url_to_report(name, report_type: str | None = None,
    doctype: str | None = None) -> str'
  body: |-
    Return the absolute URL for the report in the desk.

    e.g. when name="Sales Register" and your site URL is "https://frappe.io",
             returns 'https://frappe.io/desk/query-report/Sales%20Register'

    You can optionally pass `report_type` and `doctype` to get the URL for a Report Builder report.

    get_url_to_report("Revenue", "Report Builder", "Sales Invoice") -> 'https://frappe.io/desk/sales-invoice/view/report/Revenue'
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_get_url_to_report_with_filters:
  kind: api
  title: frappe.utils.get_url_to_report_with_filters
  summary: Return the absolute URL for the report in the desk with filters.
  keywords:
  - filters
  - frappe
  - frappe.utils
  - frappe.utils.get_url_to_report_with_filters
  - get
  - get_url_to_report_with_filters
  - report
  - url
  - utils
  - with
  signature: frappe.utils.get_url_to_report_with_filters(name, filters, report_type=None,
    doctype=None)
  body: Return the absolute URL for the report in the desk with filters.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_get_user_date_format:
  kind: api
  title: frappe.utils.get_user_date_format
  summary: Get the current user date format. The result will be cached.
  keywords:
  - date
  - format
  - frappe
  - frappe.utils
  - frappe.utils.get_user_date_format
  - get
  - get_user_date_format
  - user
  - utils
  signature: frappe.utils.get_user_date_format() -> str
  body: Get the current user date format. The result will be cached.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_get_user_info_for_avatar:
  kind: api
  title: frappe.utils.get_user_info_for_avatar
  summary: Return user info for the given `user_id` suitable for use in an avatar.
  keywords:
  - avatar
  - for
  - frappe
  - frappe.utils
  - frappe.utils.get_user_info_for_avatar
  - get
  - get_user_info_for_avatar
  - info
  - user
  - utils
  signature: 'frappe.utils.get_user_info_for_avatar(user_id: str) -> frappe.utils.data._UserInfo'
  body: |-
    Return user info for the given `user_id` suitable for use in an avatar.

    e.g. {
            "email": "faris@frappe.io",
            "image": "/assets/frappe/images/ui/avatar.png",
            "name": "Faris Ansari"
    }
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_get_user_time_format:
  kind: api
  title: frappe.utils.get_user_time_format
  summary: Get the current user time format. The result will be cached.
  keywords:
  - format
  - frappe
  - frappe.utils
  - frappe.utils.get_user_time_format
  - get
  - get_user_time_format
  - time
  - user
  - utils
  signature: frappe.utils.get_user_time_format() -> str
  body: Get the current user time format. The result will be cached.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_get_weekday:
  kind: api
  title: frappe.utils.get_weekday
  summary: Return the weekday name (e.g. 'Sunday') for the given datetime like object
    (datetime.date, datetime.datetime, string).
  keywords:
  - frappe
  - frappe.utils
  - frappe.utils.get_weekday
  - get
  - get_weekday
  - utils
  - weekday
  signature: 'frappe.utils.get_weekday(datetime: str | datetime.date | datetime.datetime
    | None = None) -> str'
  body: |-
    Return the weekday name (e.g. 'Sunday') for the given datetime like object (datetime.date, datetime.datetime, string).

    If `datetime` argument is not provided, the current weekday name is returned.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_get_weekdays:
  kind: api
  title: frappe.utils.get_weekdays
  summary: Return a list of weekday names.
  keywords:
  - frappe
  - frappe.utils
  - frappe.utils.get_weekdays
  - get
  - get_weekdays
  - utils
  - weekdays
  signature: frappe.utils.get_weekdays() -> list[str]
  body: |-
    Return a list of weekday names.

    Return value: ['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday', 'Sunday']
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_get_year_ending:
  kind: api
  title: frappe.utils.get_year_ending
  summary: Return the end date of the year for the given datetime like object (`date`).
  keywords:
  - ending
  - frappe
  - frappe.utils
  - frappe.utils.get_year_ending
  - get
  - get_year_ending
  - utils
  - year
  signature: 'frappe.utils.get_year_ending(date: str | datetime.date | datetime.datetime
    | None = None, as_str=False) -> datetime.date | str'
  body: |-
    Return the end date of the year for the given datetime like object (`date`).

    If `date` is None, the current year end date is returned.
    If `as_str` is True, the end date of the year is returned as a string in `yyyy-mm-dd` format.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_get_year_start:
  kind: api
  title: frappe.utils.get_year_start
  summary: Return the start date of the year for the given date (`dt`).
  keywords:
  - frappe
  - frappe.utils
  - frappe.utils.get_year_start
  - get
  - get_year_start
  - start
  - utils
  - year
  signature: 'frappe.utils.get_year_start(dt: str | datetime.date | datetime.datetime,
    as_str=False) -> str | datetime.date'
  body: Return the start date of the year for the given date (`dt`).
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_getdate:
  kind: api
  title: frappe.utils.getdate
  summary: |
    Parse a string / datetime-like value into a date object. Handles common Frappe date formats and site preferences.
  signature: frappe.utils.getdate(string_date=None, parse_day_first=False) -> date
  keywords:
  - getdate
  - parse
  - date
  - convert
  body: |
    d = frappe.utils.getdate("2026-04-16")      # -> date(2026, 4, 16)
    d = frappe.utils.getdate(doc.date_of_birth)  # -> date object

    If you pass a date / datetime, it's returned unchanged. Pass None to
    get today's date in one call.
  verified_on: '2026-04-16'

api_frappe_utils_global_date_format:
  kind: api
  title: frappe.utils.global_date_format
  summary: Return localized date in the form of 'January 1, 2012'.
  keywords:
  - date
  - format
  - frappe
  - frappe.utils
  - frappe.utils.global_date_format
  - global
  - global_date_format
  - utils
  signature: 'frappe.utils.global_date_format(date: str | datetime.date | datetime.datetime,
    format=''long'') -> str'
  body: Return localized date in the form of 'January 1, 2012'.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_has_common:
  kind: api
  title: frappe.utils.has_common
  summary: Return truthy value if there are common elements in lists l1 and l2.
  keywords:
  - common
  - frappe
  - frappe.utils
  - frappe.utils.has_common
  - has
  - has_common
  - utils
  signature: 'frappe.utils.has_common(l1: Hashable, l2: Hashable) -> bool'
  body: Return truthy value if there are common elements in lists l1 and l2.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_in_words:
  kind: api
  title: frappe.utils.in_words
  summary: Return string in words for the given integer.
  keywords:
  - frappe
  - frappe.utils
  - frappe.utils.in_words
  - in_words
  - utils
  - words
  signature: 'frappe.utils.in_words(integer: int, in_million=True) -> str'
  body: Return string in words for the given integer.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_is_html:
  kind: api
  title: frappe.utils.is_html
  summary: Return True if the given `text` contains any HTML tags.
  keywords:
  - frappe
  - frappe.utils
  - frappe.utils.is_html
  - html
  - is_html
  - utils
  signature: 'frappe.utils.is_html(text: str) -> bool'
  body: Return True if the given `text` contains any HTML tags.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_is_image:
  kind: api
  title: frappe.utils.is_image
  summary: Return True if the given `filepath` points to an image file.
  keywords:
  - frappe
  - frappe.utils
  - frappe.utils.is_image
  - image
  - is_image
  - utils
  signature: 'frappe.utils.is_image(filepath: str) -> bool'
  body: Return True if the given `filepath` points to an image file.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_is_invalid_date_string:
  kind: api
  title: frappe.utils.is_invalid_date_string
  summary: Return True if the date string is invalid or None or empty.
  keywords:
  - date
  - frappe
  - frappe.utils
  - frappe.utils.is_invalid_date_string
  - invalid
  - is_invalid_date_string
  - string
  - utils
  signature: 'frappe.utils.is_invalid_date_string(date_string: str) -> bool'
  body: Return True if the date string is invalid or None or empty.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_is_subset:
  kind: api
  title: frappe.utils.is_subset
  summary: Return whether list_a is a subset of list_b.
  keywords:
  - frappe
  - frappe.utils
  - frappe.utils.is_subset
  - is_subset
  - subset
  - utils
  signature: 'frappe.utils.is_subset(list_a: list, list_b: list) -> bool'
  body: Return whether list_a is a subset of list_b.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_make_filter_dict:
  kind: api
  title: frappe.utils.make_filter_dict
  summary: 'convert this [[doctype, key, operator, value], ..] to this { key: (operator,
    value), .. }'
  keywords:
  - dict
  - filter
  - frappe
  - frappe.utils
  - frappe.utils.make_filter_dict
  - make
  - make_filter_dict
  - utils
  signature: frappe.utils.make_filter_dict(filters)
  body: |-
    convert this [[doctype, key, operator, value], ..]
    to this { key: (operator, value), .. }
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_make_filter_tuple:
  kind: api
  title: frappe.utils.make_filter_tuple
  summary: return a filter tuple like [doctype, key, operator, value]
  keywords:
  - filter
  - frappe
  - frappe.utils
  - frappe.utils.make_filter_tuple
  - make
  - make_filter_tuple
  - tuple
  - utils
  signature: frappe.utils.make_filter_tuple(doctype, key, value)
  body: return a filter tuple like [doctype, key, operator, value]
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_markdown:
  kind: api
  title: frappe.utils.markdown
  summary: Convert the given markdown text to HTML and returns it.
  keywords:
  - frappe
  - frappe.utils
  - frappe.utils.markdown
  - markdown
  - utils
  signature: frappe.utils.markdown(...)
  body: Convert the given markdown text to HTML and returns it.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_md_to_html:
  kind: api
  title: frappe.utils.md_to_html
  summary: Convert the given markdown text to HTML and returns it.
  keywords:
  - frappe
  - frappe.utils
  - frappe.utils.md_to_html
  - html
  - md_to_html
  - utils
  signature: frappe.utils.md_to_html(...)
  body: Convert the given markdown text to HTML and returns it.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_money_in_words:
  kind: api
  title: frappe.utils.money_in_words
  summary: Return string in words with currency and fraction currency.
  keywords:
  - frappe
  - frappe.utils
  - frappe.utils.money_in_words
  - money
  - money_in_words
  - utils
  - words
  signature: 'frappe.utils.money_in_words(number: str | float | int, main_currency:
    str | None = None, fraction_currency: str | None = None)'
  body: Return string in words with currency and fraction currency.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_month_diff:
  kind: api
  title: frappe.utils.month_diff
  summary: Return the difference between given two dates in months.
  keywords:
  - diff
  - frappe
  - frappe.utils
  - frappe.utils.month_diff
  - month
  - month_diff
  - utils
  signature: 'frappe.utils.month_diff(string_ed_date: str | datetime.date | datetime.datetime,
    string_st_date: str | datetime.date | datetime.datetime) -> int'
  body: Return the difference between given two dates in months.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_new_line_sep:
  kind: api
  title: frappe.utils.new_line_sep
  summary: Return the given list or tuple as a new line separated string.
  keywords:
  - frappe
  - frappe.utils
  - frappe.utils.new_line_sep
  - line
  - new
  - new_line_sep
  - sep
  - utils
  signature: 'frappe.utils.new_line_sep(some_list: list | tuple) -> str'
  body: "Return the given list or tuple as a new line separated string.\n\n      \
    \ e.g. ['', 'Paid', 'Unpaid'] -> '\nPaid\nUnpaid'\n       "
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_now:
  kind: api
  title: frappe.utils.now
  summary: Return current datetime as `yyyy-mm-dd hh:mm:ss`.
  keywords:
  - frappe
  - frappe.utils
  - frappe.utils.now
  - now
  - utils
  signature: frappe.utils.now() -> str
  body: Return current datetime as `yyyy-mm-dd hh:mm:ss`.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_now_datetime:
  kind: api
  title: frappe.utils.now_datetime
  summary: |
    Current datetime (aware) in the site's timezone. Use instead of datetime.datetime.now() - the site-timezone behaviour is what matters for scheduled jobs and audit timestamps.
  signature: frappe.utils.now_datetime() -> datetime
  keywords:
  - now_datetime
  - now
  - datetime
  - timestamp
  - current
  body: |
    ts = frappe.utils.now_datetime()
    # -> datetime in site's configured timezone
  examples:
    good: |
      doc.processed_at = frappe.utils.now_datetime()
  related:
  - api_frappe_utils_nowdate
  verified_on: '2026-04-16'

api_frappe_utils_nowdate:
  kind: api
  title: frappe.utils.nowdate
  summary: |
    Today's date as an ISO string (YYYY-MM-DD) in the site's timezone. Use instead of `datetime.date.today()` to respect the site's config.
  signature: frappe.utils.nowdate() -> str
  keywords:
  - nowdate
  - today
  - date
  - current
  body: |
    today_str = frappe.utils.nowdate()   # e.g. "2026-04-16"

    Returns a string, not a date object. For date math, pair with
    frappe.utils.getdate / add_days / date_diff.
  examples:
    good: |
      if frappe.utils.date_diff(frappe.utils.nowdate(), doc.dob) < 24 * 365:
          frappe.throw("Employee must be at least 24 years old")
    bad: |
      # Don't use stdlib datetime in Server Scripts (no `import` allowed)
      import datetime
      if datetime.date.today() - doc.dob < timedelta(days=24*365):
          ...
  related:
  - api_frappe_utils_getdate
  - api_frappe_utils_date_diff
  - server_script_no_imports
  verified_on: '2026-04-16'

api_frappe_utils_nowtime:
  kind: api
  title: frappe.utils.nowtime
  summary: Return current time (system timezone) in `hh:mm:ss` format.
  keywords:
  - frappe
  - frappe.utils
  - frappe.utils.nowtime
  - nowtime
  - utils
  signature: frappe.utils.nowtime() -> str
  body: Return current time (system timezone) in `hh:mm:ss` format.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_orjson_dumps:
  kind: api
  title: frappe.utils.orjson_dumps
  summary: A wrapper around `orjson.dumps`, with some default options set
  keywords:
  - dumps
  - frappe
  - frappe.utils
  - frappe.utils.orjson_dumps
  - orjson
  - orjson_dumps
  - utils
  signature: frappe.utils.orjson_dumps(obj, default=None, option=None, decode=True)
  body: A wrapper around `orjson.dumps`, with some default options set
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_parse_json:
  kind: api
  title: frappe.utils.parse_json
  summary: Parses json if string else return
  keywords:
  - frappe
  - frappe.utils
  - frappe.utils.parse_json
  - json
  - parse
  - parse_json
  - utils
  signature: 'frappe.utils.parse_json(val: str)'
  body: Parses json if string else return
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_parse_val:
  kind: api
  title: frappe.utils.parse_val
  summary: Convert to simple datatypes from SQL query results.
  keywords:
  - frappe
  - frappe.utils
  - frappe.utils.parse_val
  - parse
  - parse_val
  - utils
  - val
  signature: frappe.utils.parse_val(v)
  body: Convert to simple datatypes from SQL query results.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_pretty_date:
  kind: api
  title: frappe.utils.pretty_date
  summary: Return a localized string representation of the delta to the current system
    time.
  keywords:
  - date
  - frappe
  - frappe.utils
  - frappe.utils.pretty_date
  - pretty
  - pretty_date
  - utils
  signature: 'frappe.utils.pretty_date(iso_datetime: datetime.datetime | str, mini=False)
    -> str'
  body: |-
    Return a localized string representation of the delta to the current system time.

    For example, "1 hour ago", "2 days ago", "in 5 seconds", etc.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_quote_urls:
  kind: api
  title: frappe.utils.quote_urls
  summary: (Auto-scraped from frappe.utils; no upstream docstring.)
  keywords:
  - frappe
  - frappe.utils
  - frappe.utils.quote_urls
  - quote
  - quote_urls
  - urls
  - utils
  signature: 'frappe.utils.quote_urls(html: str) -> str'
  body: (Auto-scraped from frappe.utils; no upstream docstring.)
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_quoted:
  kind: api
  title: frappe.utils.quoted
  summary: Return the given `url` quoted.
  keywords:
  - frappe
  - frappe.utils
  - frappe.utils.quoted
  - quoted
  - utils
  signature: 'frappe.utils.quoted(url: str) -> str'
  body: |-
    Return the given `url` quoted.

    e.g. 'https://frappe.io/files/my Image file.jpeg' -> 'https://frappe.io/files/my%20Image%20file.jpeg'
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_remainder:
  kind: api
  title: frappe.utils.remainder
  summary: Return the remainder of the division of `numerator` by `denominator`.
  keywords:
  - frappe
  - frappe.utils
  - frappe.utils.remainder
  - remainder
  - utils
  signature: 'frappe.utils.remainder(numerator: int | float, denominator: int | float,
    precision: int = 2) -> int | float'
  body: Return the remainder of the division of `numerator` by `denominator`.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_round_based_on_smallest_currency_fraction:
  kind: api
  title: frappe.utils.round_based_on_smallest_currency_fraction
  summary: (Auto-scraped from frappe.utils; no upstream docstring.)
  keywords:
  - based
  - currency
  - fraction
  - frappe
  - frappe.utils
  - frappe.utils.round_based_on_smallest_currency_fraction
  - round
  - round_based_on_smallest_currency_fraction
  - smallest
  - utils
  signature: frappe.utils.round_based_on_smallest_currency_fraction(value, currency,
    precision=2)
  body: (Auto-scraped from frappe.utils; no upstream docstring.)
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_rounded:
  kind: api
  title: frappe.utils.rounded
  summary: Round according to method set in system setting, defaults to banker's rounding
  keywords:
  - frappe
  - frappe.utils
  - frappe.utils.rounded
  - rounded
  - utils
  signature: frappe.utils.rounded(num, precision=0, rounding_method=None)
  body: Round according to method set in system setting, defaults to banker's rounding
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_safe_div:
  kind: api
  title: frappe.utils.safe_div
  summary: SafeMath division that returns zero when divided by zero.
  keywords:
  - div
  - frappe
  - frappe.utils
  - frappe.utils.safe_div
  - safe
  - safe_div
  - utils
  signature: 'frappe.utils.safe_div(numerator: int | float, denominator: int | float,
    precision: int = 2) -> float'
  body: SafeMath division that returns zero when divided by zero.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_sanitize_column:
  kind: api
  title: frappe.utils.sanitize_column
  summary: (Auto-scraped from frappe.utils; no upstream docstring.)
  keywords:
  - column
  - frappe
  - frappe.utils
  - frappe.utils.sanitize_column
  - sanitize
  - sanitize_column
  - utils
  signature: 'frappe.utils.sanitize_column(column_name: str) -> str'
  body: (Auto-scraped from frappe.utils; no upstream docstring.)
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_scrub_urls:
  kind: api
  title: frappe.utils.scrub_urls
  summary: Expand relative urls in the given `html`.
  keywords:
  - frappe
  - frappe.utils
  - frappe.utils.scrub_urls
  - scrub
  - scrub_urls
  - urls
  - utils
  signature: 'frappe.utils.scrub_urls(html: str) -> str'
  body: |-
    Expand relative urls in the given `html`.

    e.g. If HTML is '<a href="/files/abc.jpeg">View Image</a>' and site URL is 'https://frappe.io',
            returns '<a href="https://frappe.io/files/abc.jpeg">View Image</a>'.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_sha256_hash:
  kind: api
  title: frappe.utils.sha256_hash
  summary: Return hash of the string using sha256 algorithm.
  keywords:
  - frappe
  - frappe.utils
  - frappe.utils.sha256_hash
  - hash
  - sha256
  - sha256_hash
  - utils
  signature: 'frappe.utils.sha256_hash(input: str | bytes) -> str'
  body: Return hash of the string using sha256 algorithm.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_strip:
  kind: api
  title: frappe.utils.strip
  summary: Strip the given characters from the given string.
  keywords:
  - frappe
  - frappe.utils
  - frappe.utils.strip
  - strip
  - utils
  signature: 'frappe.utils.strip(val: str, chars: str | None = None) -> str'
  body: |-
    Strip the given characters from the given string.

    e.g. strip(',hello,bye,', ',') -> 'hello,bye'
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_strip_html:
  kind: api
  title: frappe.utils.strip_html
  summary: Remove anything enclosed in and including <>.
  keywords:
  - frappe
  - frappe.utils
  - frappe.utils.strip_html
  - html
  - strip
  - strip_html
  - utils
  signature: 'frappe.utils.strip_html(text: str) -> str'
  body: Remove anything enclosed in and including <>.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_time_diff:
  kind: api
  title: frappe.utils.time_diff
  summary: Return the difference between given two dates as `datetime.timedelta` object.
  keywords:
  - diff
  - frappe
  - frappe.utils
  - frappe.utils.time_diff
  - time
  - time_diff
  - utils
  signature: 'frappe.utils.time_diff(string_ed_date: str | datetime.date | datetime.datetime,
    string_st_date: str | datetime.date | datetime.datetime) -> datetime.timedelta'
  body: Return the difference between given two dates as `datetime.timedelta` object.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_time_diff_in_hours:
  kind: api
  title: frappe.utils.time_diff_in_hours
  summary: Return the difference between given two dates in hours.
  keywords:
  - diff
  - frappe
  - frappe.utils
  - frappe.utils.time_diff_in_hours
  - hours
  - time
  - time_diff_in_hours
  - utils
  signature: 'frappe.utils.time_diff_in_hours(string_ed_date: str | datetime.date
    | datetime.datetime, string_st_date: str | datetime.date | datetime.datetime)
    -> float'
  body: Return the difference between given two dates in hours.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_time_diff_in_seconds:
  kind: api
  title: frappe.utils.time_diff_in_seconds
  summary: Return the difference between given two dates in seconds.
  keywords:
  - diff
  - frappe
  - frappe.utils
  - frappe.utils.time_diff_in_seconds
  - seconds
  - time
  - time_diff_in_seconds
  - utils
  signature: 'frappe.utils.time_diff_in_seconds(string_ed_date: str | datetime.date
    | datetime.datetime, string_st_date: str | datetime.date | datetime.datetime)
    -> float'
  body: Return the difference between given two dates in seconds.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_to_markdown:
  kind: api
  title: frappe.utils.to_markdown
  summary: Convert the given HTML to markdown and returns it.
  keywords:
  - frappe
  - frappe.utils
  - frappe.utils.to_markdown
  - markdown
  - to_markdown
  - utils
  signature: 'frappe.utils.to_markdown(html: str) -> str'
  body: Convert the given HTML to markdown and returns it.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_to_timedelta:
  kind: api
  title: frappe.utils.to_timedelta
  summary: Return a `datetime.timedelta` object from the given string or `datetime.time`
    object. If the given argument is not a string or a `datetime.time` object, it
    is returned as is.
  keywords:
  - frappe
  - frappe.utils
  - frappe.utils.to_timedelta
  - timedelta
  - to_timedelta
  - utils
  signature: 'frappe.utils.to_timedelta(time_str: str | datetime.time) -> datetime.timedelta'
  body: |-
    Return a `datetime.timedelta` object from the given string or `datetime.time` object.
    If the given argument is not a string or a `datetime.time` object, it is returned as is.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_today:
  kind: api
  title: frappe.utils.today
  summary: Return today's date in `yyyy-mm-dd` format.
  keywords:
  - frappe
  - frappe.utils
  - frappe.utils.today
  - today
  - utils
  signature: frappe.utils.today() -> str
  body: Return today's date in `yyyy-mm-dd` format.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_unique:
  kind: api
  title: frappe.utils.unique
  summary: 'use this instead of list(set()) to preserve order of the original list.
    Thanks to Stackoverflow: http://stackoverflow.com/questions/480214/how-do-you-remove-duplicates-from-a-list-in-python-whilst-preserving-order'
  keywords:
  - frappe
  - frappe.utils
  - frappe.utils.unique
  - unique
  - utils
  signature: 'frappe.utils.unique(seq: Sequence[ForwardRef(''T'')]) -> list[''T'']'
  body: |-
    use this instead of list(set()) to preserve order of the original list.
    Thanks to Stackoverflow: http://stackoverflow.com/questions/480214/how-do-you-remove-duplicates-from-a-list-in-python-whilst-preserving-order
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'

api_frappe_utils_url_contains_port:
  kind: api
  title: frappe.utils.url_contains_port
  summary: Return True if the given url contains a port number.
  keywords:
  - contains
  - frappe
  - frappe.utils
  - frappe.utils.url_contains_port
  - port
  - url
  - url_contains_port
  - utils
  signature: 'frappe.utils.url_contains_port(url: str) -> bool'
  body: |-
    Return True if the given url contains a port number.

    e.g. 'http://localhost:8000' -> True, 'http://localhost' -> False.
  source: auto-scraped from frappe v17.0.0-dev
  verified_on: '2026-04-16'
